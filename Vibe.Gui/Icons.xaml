<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- ======= Oslo Palette Brushes (tweak as you like) ======= -->
  <!-- Dominant cool blue -->
  <SolidColorBrush x:Key="Oslo.Blueprint" Color="#365F91"/>
  <!-- Light bluish grays -->
  <SolidColorBrush x:Key="Oslo.FjordMist" Color="#AABBD3"/>
  <SolidColorBrush x:Key="Oslo.GlacierHaze" Color="#CCD5E1"/>
  <!-- Neutrals -->
  <SolidColorBrush x:Key="Oslo.White" Color="#FFFFFF"/>
  <SolidColorBrush x:Key="Oslo.Graphite" Color="#666666"/>
  <!-- Olive/lichen accent (for “native”/transform motif) -->
  <SolidColorBrush x:Key="Oslo.Lichen" Color="#C6DC1D"/>
  <SolidColorBrush x:Key="Oslo.Moss" Color="#B6CA59"/>

  <!-- Shared pens (use device‑independent px; line caps for crispness) -->
  <Pen x:Key="IconStrokeThin" Brush="{StaticResource Oslo.Graphite}" Thickness="1" StartLineCap="Square" EndLineCap="Square"/>
  <Pen x:Key="IconStrokeMed"  Brush="{StaticResource Oslo.Graphite}" Thickness="1.25" StartLineCap="Square" EndLineCap="Square"/>
  <Pen x:Key="IconStrokeBlue" Brush="{StaticResource Oslo.Blueprint}" Thickness="1.25" StartLineCap="Square" EndLineCap="Square"/>

  <!-- ========================================================= -->
  <!-- 1) APP ICON: “Chip → Structured Blocks” (no letters)     -->
  <!--    A microchip feeds an arrow into three tidy blocks,     -->
  <!--    i.e., native → structured high-level representation.   -->
  <!--    Designed to read well down to 16x16.                   -->
  <!-- ========================================================= -->
  <DrawingImage x:Key="AppIconImage">
    <DrawingImage.Drawing>
      <DrawingGroup>
        <!-- Chip body -->
        <GeometryDrawing Brush="{StaticResource Oslo.Blueprint}">
          <GeometryDrawing.Geometry>
            <RectangleGeometry Rect="2,6,8,12" RadiusX="1" RadiusY="1"/>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>

        <!-- Chip pins (left and right) -->
        <GeometryDrawing Brush="{StaticResource Oslo.Graphite}">
          <GeometryDrawing.Geometry>
            <GeometryGroup>
              <!-- Left pins -->
              <RectangleGeometry Rect="0.5,8,1.5,2"/>
              <RectangleGeometry Rect="0.5,12,1.5,2"/>
              <RectangleGeometry Rect="0.5,16,1.5,2"/>
              <!-- Right pins -->
              <RectangleGeometry Rect="10.0,8,1.5,2"/>
              <RectangleGeometry Rect="10.0,12,1.5,2"/>
              <RectangleGeometry Rect="10.0,16,1.5,2"/>
            </GeometryGroup>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>

        <!-- Transform arrow (chip → right) -->
        <!-- shaft -->
        <GeometryDrawing Brush="{StaticResource Oslo.Lichen}">
          <GeometryDrawing.Geometry>
            <RectangleGeometry Rect="12,11,7,2"/>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
        <!-- head -->
        <GeometryDrawing Brush="{StaticResource Oslo.Lichen}">
          <GeometryDrawing.Geometry>
            <PathGeometry Figures="M19,9 L23,12 L19,15 Z"/>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>

        <!-- Structured blocks (3 panels) -->
        <GeometryDrawing Brush="{StaticResource Oslo.GlacierHaze}" Pen="{StaticResource IconStrokeThin}">
          <GeometryDrawing.Geometry>
            <GeometryGroup>
              <RectangleGeometry Rect="16,4,6,3" RadiusX="0.5" RadiusY="0.5"/>
              <RectangleGeometry Rect="16,10.5,6,3" RadiusX="0.5" RadiusY="0.5"/>
              <RectangleGeometry Rect="16,17,6,3" RadiusX="0.5" RadiusY="0.5"/>
            </GeometryGroup>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
      </DrawingGroup>
    </DrawingImage.Drawing>
  </DrawingImage>

  <!-- ========================================================= -->
  <!-- 2) NATIVE DLL ICON (TreeView): “Document + Chip badge”   -->
  <!--    Document with folded corner; chip badge = native code. -->
  <!-- ========================================================= -->
  <DrawingImage x:Key="DllIconImage">
    <DrawingImage.Drawing>
      <DrawingGroup>
        <!-- Document shape with fold -->
        <GeometryDrawing Brush="{StaticResource Oslo.White}" Pen="{StaticResource IconStrokeThin}">
          <GeometryDrawing.Geometry>
            <!-- Document outline (folded top-right corner) -->
            <PathGeometry Figures="M6,4 L16,4 L20,8 L20,20 L6,20 Z"/>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
        <!-- Fold highlight -->
        <GeometryDrawing Brush="{StaticResource Oslo.GlacierHaze}">
          <GeometryDrawing.Geometry>
            <PathGeometry Figures="M16,4 L20,8 L16,8 Z"/>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>

        <!-- Chip badge (bottom-right) -->
        <GeometryDrawing Brush="{StaticResource Oslo.Blueprint}" Pen="{StaticResource IconStrokeBlue}">
          <GeometryDrawing.Geometry>
            <RectangleGeometry Rect="12,14,8,6" RadiusX="1" RadiusY="1"/>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>

        <!-- Chip pins (top & bottom) -->
        <GeometryDrawing Brush="{StaticResource Oslo.Graphite}">
          <GeometryDrawing.Geometry>
            <GeometryGroup>
              <!-- Top pins -->
              <RectangleGeometry Rect="13,13,1,1"/>
              <RectangleGeometry Rect="15,13,1,1"/>
              <RectangleGeometry Rect="17,13,1,1"/>
              <!-- Bottom pins -->
              <RectangleGeometry Rect="13,20,1,1"/>
              <RectangleGeometry Rect="15,20,1,1"/>
              <RectangleGeometry Rect="17,20,1,1"/>
            </GeometryGroup>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
      </DrawingGroup>
    </DrawingImage.Drawing>
  </DrawingImage>

  <!-- ========================================================= -->
  <!-- 3) NATIVE EXPORTED FUNCTION ICON (TreeView): “λ in yellow” -->
  <!--    Simple lambda symbol rendered in a warm yellow.        -->
  <!-- ========================================================= -->
  <SolidColorBrush x:Key="Lambda.Native" Color="#E7D27B"/>
  <SolidColorBrush x:Key="Lambda.Managed" Color="#9DB463"/>
  <Pen x:Key="LambdaNativePen" Brush="{StaticResource Lambda.Native}" Thickness="1.6" StartLineCap="Round" EndLineCap="Round"/>
  <Pen x:Key="LambdaManagedPen" Brush="{StaticResource Lambda.Managed}" Thickness="1.6" StartLineCap="Round" EndLineCap="Round"/>

  <DrawingImage x:Key="NativeFunctionIconImage">
    <DrawingImage.Drawing>
      <DrawingGroup>
        <GeometryDrawing Brush="{x:Null}" Pen="{StaticResource LambdaNativePen}">
          <GeometryDrawing.Geometry>
            <PathGeometry Figures="M7,3 L4,15 M7,3 C10,9 11,12 13,15 L15,15"/>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
      </DrawingGroup>
    </DrawingImage.Drawing>
  </DrawingImage>

  <!-- ========================================================= -->
  <!-- 4) MANAGED METHOD ICON (TreeView): “λ in green”           -->
  <!--    Lambda symbol in a calm green for managed methods.     -->
  <!-- ========================================================= -->
  <DrawingImage x:Key="ManagedMethodIconImage">
    <DrawingImage.Drawing>
      <DrawingGroup>
        <GeometryDrawing Brush="{x:Null}" Pen="{StaticResource LambdaManagedPen}">
          <GeometryDrawing.Geometry>
            <PathGeometry Figures="M7,3 L4,15 M7,3 C10,9 11,12 13,15 L15,15"/>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
      </DrawingGroup>
    </DrawingImage.Drawing>
  </DrawingImage>

</ResourceDictionary>
